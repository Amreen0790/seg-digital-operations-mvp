# seg-digital-operations-mvp
simple static MVP demo that can be hosted on GitHub Pages. It shows the core modules: dashboard, job management, scheduling, field execution, documents, and master data. This is only a prototype, not the final deployed system
